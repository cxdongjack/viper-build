/* exported document */
var document = window.document;

/* exported history */
var history = window.history;

/* exported location */
var location = window.location;

/* exported navigator */
var navigator = window.navigator;

/* exported console */
var console = window.console;

/* exported alert */
var alert = window.alert;

/* exported confirm */
var confirm = window.confirm;

/* exported Image */
var Image = window.Image;

/* exported setTimeout */
var setTimeout = window.setTimeout;

/* exported clearTimeout */
var clearTimeout = window.clearTimeout;

/* exported setInterval */
var setInterval = window.setInterval;

/* exported clearInterval */
var clearInterval = window.clearInterval;

/* exported getComputedStyle */
var getComputedStyle = window.getComputedStyle;

/* exported encodeURIComponent */
var encodeURIComponent = window.encodeURIComponent;

/* exported decodeURIComponent */
var decodeURIComponent = window.decodeURIComponent;

/* exported localStorage */
var localStorage = window.localStorage;

/* exported sessionStorage */
var sessionStorage = window.sessionStorage;

/* exported String */
var String = window.String;

/* exported Array */
var Array = window.Array;

/* exported Object */
var Object = window.Object;

/* exported Date */
var Date = window.Date;

/* exported XMLHttpRequest */
var XMLHttpRequest = window.XMLHttpRequest;

/* exported Node */
var Node = window.Node;

/* exported NodeList */
var NodeList = window.NodeList;

/* exported HTMLElement */
var HTMLElement = window.HTMLElement;

/* exported HTMLCollection */
var HTMLCollection = window.HTMLCollection;

/* exported applicationCache */
var applicationCache = window.applicationCache;
console.log('enter module-a');

function moduleA() {
    return 'moduleA';
}


function text() {
    return "some text";
}

moduleA()
console.log('success!');



var app__testLocalURI = '9oZi9@9q.json';



